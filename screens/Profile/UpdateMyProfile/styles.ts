import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        flex: 1,
        justifyContent: "center",
    },
    image: {
        borderRadius: 50,
        borderWidth: 1,
        height: 100,
        marginVertical: 10,
        width: 100,
    }
})