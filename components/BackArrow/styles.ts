import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  arrow: {
    left: 0,
    marginHorizontal: 10,
    marginVertical: 20,
    padding: 15,
    position: "absolute",
    top: 0,
  },
})